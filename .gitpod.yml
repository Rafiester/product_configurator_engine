tasks:
  - name: Next.js Setup & Start
    init: |
      npm install
      cp .env.example .env
      npx prisma generate
      npm run build
    command: |
      npm run dev

ports:
  - port: 3000
    onOpen: open-preview
    visibility: public
  - port: 3001
    onOpen: ignore
    visibility: public
